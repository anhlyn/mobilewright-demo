name: Mobilewright Test
on:
    push:
        branches: [main]
    
jobs:
    ios-test:
        runs-on: macos-26
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                node-version: 22
            - run: |
                pwd
                npm ci
            - name: Run mobilewright test
              run: npx mobilewright test